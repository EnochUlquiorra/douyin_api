# douyin_tools
douyin tools,定期更新,订阅可联系邮箱

#### 命令行启动文件
```casbin

MacOS   64位 douyin-tools-darwin-amd64
Linux   64位 douyin-tools-linux-amd64
Windows 64位 douyin-tools-windos-amd64.exe

config.toml文件配置授权key.

```

API 文档
```casbin
/api/device_register
/api/do/feed/{device_id}
/api/do/login/send_sms/{phone}/{code} 
/api/do/login/up_sms/{phone}/{ticket}
/api/do/user/favorite/{device_id}/{user_id}/{sec_user_id} 
/api/do/user/mix/aweme/{device_id}/{user_id}/{sec_user_id}/{mix_id}
/api/do/user/mix/detail/{device_id}/{user_id}/{sec_user_id}/{mix_id}
/api/do/user/mix/list/{device_id}/{user_id}/{sec_user_id} 
/api/do/user/post/{device_id}/{user_id}/{sec_user_id}
/api/do/user/profile/{device_id}/{user_id}/{sec_user_id}
/api/do/user/promotion/{device_id}/{user_id}/{sec_user_id} 
/api/do/user/search/{device_id}/{key_word}
```
